import React from 'react';
import Navbar from './Navbar';
// import './CSSLoader.css'; // Ensure this path points to the CSS file

const Loader = () => {
    return (

        <>

            <div className="loader-wrapper">
                <div className="loader"></div>
            </div></>
    );
};

export default Loader;
